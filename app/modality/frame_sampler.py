from pathlib import Path


def sample_frames(video_path: str | Path, output_dir: str | Path, every_seconds: int = 60) -> list[Path]:
    """Lightweight placeholder for future ffmpeg/OpenCV frame sampling."""
    raise NotImplementedError("Frame sampling is reserved for vision_supportive/vision_critical follow-up work.")
